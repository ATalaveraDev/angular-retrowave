import { RetroWaveAudioOutputDirectiveDirective } from './audio-output-directive.directive';

describe('RetroWaveAudioOutputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioOutputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
