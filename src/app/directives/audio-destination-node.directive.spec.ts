import { RetroWaveAudioDestinationNodeDirective } from './audio-destination-node.directive';

describe('RetroWaveAudioDestinationNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioDestinationNodeDirective();
    expect(directive).toBeTruthy();
  });
});
