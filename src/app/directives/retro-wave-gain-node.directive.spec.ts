import { RetroWaveGainNodeDirective } from './retro-wave-gain-node.directive';

describe('RetroWaveGainNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new RetroWaveGainNodeDirective();
    expect(directive).toBeTruthy();
  });
});
