import { RetroWaveAudioDestinationNodeDirective } from './retro-wave-audio-destination-node.directive';

describe('RetroWaveAudioDestinationNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new RetroWaveAudioDestinationNodeDirective();
    expect(directive).toBeTruthy();
  });
});
