import { RetroWaveGainNodeDirective } from './gain-node.directive';

describe('GainNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new GainNodeDirective();
    expect(directive).toBeTruthy();
  });
});
