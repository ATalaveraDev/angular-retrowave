import { AudioMediaSourceDirective } from './audio-media-source.directive';

describe('AudioMediaSourceDirective', () => {
  it('should create an instance', () => {
    const directive = new AudioMediaSourceDirective();
    expect(directive).toBeTruthy();
  });
});
