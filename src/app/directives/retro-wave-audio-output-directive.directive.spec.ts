import { RetroWaveAudioOutputDirectiveDirective } from './retro-wave-audio-output-directive.directive';

describe('RetroWaveAudioOutputDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new RetroWaveAudioOutputDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
