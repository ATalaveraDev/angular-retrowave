import { DelayNodeDirective } from './delay-node.directive';

describe('DelayNodeDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayNodeDirective();
    expect(directive).toBeTruthy();
  });
});
